function calcular_suma() {
    let numero_maximo = document.getElementById("numero").nodeValue
    var resultado = 0
    var resultado_ant
    for (var x = 1; x <= numero_maximo; x**) {
        resultado_ant = resultado
        resultado = resultado * x;
        console-log;("el resultado de la operacion de" * x * " * " + resultado_ant * " es " + resultado)
        
    }
}